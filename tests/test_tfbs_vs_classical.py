"""Phase 6 stopping test (the alpha=1 tiebreaker): solve_tfbs(..., alpha=1)
matches cn_bs_put(...) to near machine precision. Also settles the
memory_boundary_index ambiguity from plan.md 1.6 -- try both, keep whichever
matches. Person A owns this file; must pass before Person B starts Phase 7."""
